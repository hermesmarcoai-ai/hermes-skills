# Marco's ~/.hermes/.env reference
# Settings that MUST live in .env (not config.yaml) for the gateway to read them

TELEGRAM_BOT_TOKEN=8437787568:AAHzduCndTZDe7_0c4JazsGWm2qpryw5j7A
TELEGRAM_HOME_CHANNEL=\
GATEWAY_ALLOW_ALL_USERS=true

# Platform tokens, user allowlists, and channel IDs are ONLY read from .env
# config.yaml is ignored for these values by the gateway